<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <main class="wrap">
    <h1><%= title %></h1>

    <p class="status">
      Emotion: <strong><%= state.emotion || '—' %></strong> |
      Priority: <strong><%= state.priority || '—' %></strong> |
      Food: <strong><%= state.food || '—' %></strong>
    </p>

    <% if (step == 1) { %>
      <form action="/quiz" method="POST">
        <h2>Question 1: What's your emotion today?</h2>
        <input type="radio" name="emotion" value="chaotic" required /> Chaotic<br />
        <input type="radio" name="emotion" value="emotion" /> Emotionally Unavailable<br />
        <input type="radio" name="emotion" value="content" /> Content<br />
        <input type="radio" name="emotion" value="hungry" /> Hungry<br />
        <button type="submit">Next</button>
      </form>
    <% } %>
    <% if (step == 2) { %>
      <form action="/quiz" method="POST"> 
        <h2>Question 2: What is your priority?</h2>
        <input type="radio" name="priority" value="family" required /> Family<br />
        <input type="radio" name="priority" value="fame" /> Fame<br />
        <input type="radio" name="priority" value="fortune" /> Fortune<br />
        <input type="radio" name="priority" value="love" /> Love<br />
        <input type="hidden" name="emotion" value="<%= state.emotion %>" />
        <button type="submit">Next</button>
      </form>
    <% } %>
    <% if (step == 3) { %>
      <form action="/quiz" method="POST"> 
        <h2>Question 3: What food encapsulates your aura?</h2>
        <input type="radio" name="food" value="spaghetti" required /> Spaghetti<br />
        <input type="radio" name="food" value="fillet" /> Fillet Mingnon<br />
        <input type="radio" name="food" value="Pancakes" /> Pancakes<br />
        <input type="radio" name="food" value="bread" /> Bread<br />
        <input type="hidden" name="priority" value="<%= state.priority %>" />
        <input type="hidden" name="emotion" value="<%= state.emotion %>" />
        <button type="submit">Reveal Outcome</button>
      </form>
    <% } %>
    <% if (step === 4) { %>
      <h2>Outcome</h2>
      <div class="card">
        <h3><%= outcome.heading %></h3>
        <p><%= outcome.text %></p>
        <form action="/" method="get">
          <button type="submit">Play Again</button>
        </form>
      </div>      
    <% } %>
  </main>
</body>
</html>
